import type { ReactNode } from 'react'

/**
 * Tools route group layout — document scroll only (no landing viewport lock).
 */
export default function ToolsLayout({ children }: { children: ReactNode }) {
  return children
}
