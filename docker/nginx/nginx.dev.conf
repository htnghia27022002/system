include /etc/nginx/config/upstreams.dev.conf;

server {
    listen 80;
    server_name system.local localhost 127.0.0.1;

    client_max_body_size 20m;

    include /etc/nginx/config/be.conf;
    include /etc/nginx/config/fe.dev.conf;
}
